#include <stdlib.h>
#include <stdio.h>

int main()
{
	int x;
	int y;
	char signe;
	printf("écrivez un nombre entier :\n");
        scanf("%d", &x);
        printf("écrivez un second nombre entier :\n");
        scanf("%d", &y);
        printf("choisissez un opérateur (+, *, /, -, %) :\n");
        scanf("%c", &signe)
	exit(0)
}
